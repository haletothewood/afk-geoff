{
  "name": "afk-geoff",
  "version": "0.1.2",
  "description": "A boringly reliable execution layer for autonomous coding agents, with isolated runs, verification, review, and PR handoff.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/haletothewood/afk-geoff.git"
  },
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "afk": "bin/afk.js"
  },
  "main": "./dist/bundle/index.js",
  "exports": "./dist/bundle/index.js",
  "types": "./dist/bundle/index.d.ts",
  "files": [
    "bin",
    "dist/bundle",
    "dist/templates"
  ],
  "scripts": {
    "build": "tsc --build && node ../../scripts/build-cli-bundle.mjs",
    "prepack": "pnpm build"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "commander": "^14.0.1"
  }
}
